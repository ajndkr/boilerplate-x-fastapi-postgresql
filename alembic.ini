# Alembic configuration file for database migrations

[alembic]
script_location = alembic/versions
sqlalchemy.url = postgresql://user:password@localhost:5432/chat_db

[logging]
fileConfig = /path/to/logging.conf

[version_locations]
versions = alembic/versions